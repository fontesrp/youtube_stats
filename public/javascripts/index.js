(function () {

    "use strict";

    const onload = function () {

        setupNavBar();
        setupBroadcasts();

        setupUserMessages();
    };

    document.addEventListener("DOMContentLoaded", onload);
}());
